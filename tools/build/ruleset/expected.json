{
  "comment": "Required branch protection rules for every keepass-web repository. The CI ruleset check validates that all of these rule types are active on the default branch, and that any rules with a 'parameters' object are configured at least as strictly as specified there. Rules are created manually once per the process documented in RULESETS.md; CI validates them on every run.",
  "required_rules": [
    {
      "type": "deletion"
    },
    {
      "type": "non_fast_forward"
    },
    {
      "type": "pull_request"
    },
    {
      "type": "required_signatures"
    },
    {
      "type": "code_scanning",
      "parameters": {
        "code_scanning_tools": [
          {
            "tool": "CodeQL",
            "alerts_threshold": "all",
            "security_alerts_threshold": "all"
          }
        ]
      }
    },
    {
      "type": "required_status_checks"
    }
  ]
}
