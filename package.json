{
  "name": "source-application",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "KeePass Web — source monorepo. Builds self-contained HTML distributables for a KDBX password manager that runs entirely in the browser.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "tools/build",
    "pages"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/node": "^22.20.0",
    "typescript": "6.0.3"
  },
  "scripts": {
    "clean": "rm -rf build dist",
    "build:deps": "npm run build --workspace=argon2 --workspace=chacha20 --workspace=kdbx",
    "build": "npm run build:deps && npm run build --workspace=pages",
    "typecheck": "npm run build:deps && npm run typecheck --workspaces --if-present",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "npm run build:deps && npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
